# Ticket: Lowercase Phonetics and 0-Indexed Piano Octaves (Landed)

## Kind
bounded — completed and landed in `b5fa55c`.

## Requirements Implemented
1. **Strictly Lowercase 12-TET Phonetics**:
   - Consonants: `m`, `d`, `v`, `p`, `l`, `r`, `n`, `t`, `f`, `b`, `s`, `k`
   - Syllables:
     - Row 0 (-a): `ma`, `va`, `la`, `na`, `fa`, `sa`
     - Row 1 (-i): `di`, `pi`, `ri`, `ti`, `bi`, `ki`
   - All notehead morphology, pitch class labels, reverse lookup, presets, and UI text strictly lowercase.
2. **0-Indexed Piano Octaves (`m0`..`m7`)**:
   - Lowest piano C (C1, MIDI 24) is `m0`.
   - Middle C (C4, MIDI 60) is `m3`.
   - Highest piano C (C8, MIDI 108) is `m7`.
   - Notes before `m0` (bottom A0, Bb0, B0) are also octave 0 (`fa0`, `bi0`, `ki0`, linear index 9..11).
3. **Rendering & Layout**:
   - Staff header labels in SVG and canvas render `m${Math.max(0, oct - 1)}`.
   - Lateral stems, optical notehead balance, and 2-page A4 print layout fully preserved and verified.

## Verification
- 62/62 tests passing cleanly in `npm test`.
- Clean production build in `npm run build`.
- Landed via fast-forward on `main` at `b5fa55ca625470d1497608d8472d587c6111e670`.
